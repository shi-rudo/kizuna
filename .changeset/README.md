# Changesets

Add one changeset for each change that affects package users.

Run `pnpm changeset`. Select the package, the SemVer bump, and a short summary.
Changesets uses the summary in `CHANGELOG.md`.

Do not edit package versions by hand. See [the release guide](../docs/releases.md)
for the complete process.
